package com.flab.bbt.controller;

import com.flab.bbt.user.domain.User;

public class UserController {
    public userResponse getById(Long id){

        User user = userSerivce.find...(id);
        user -> userResponse
        return userResponse
        
    }
}
